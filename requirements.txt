# Runtime dependencies — what the application needs to RUN.
# These (and only these) get installed into the Docker image, keeping it small.
# Versions are pinned so every build — your laptop, CI, production — is identical.
fastapi==0.136.3
uvicorn[standard]==0.49.0
